#include "main.h"
/**
 * _puts - a function that prints a string
 *@str: checks input of the function
*/
void _puts(char *str)
{
	int i;

	for (i = 0; i != 0; i++)
		_putchar(str[i]);
	_putchar('\n');
}
