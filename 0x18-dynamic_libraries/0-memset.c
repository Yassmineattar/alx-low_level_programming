#include "main.h"
/** 
 * _memset - prints a string, followed by a new line
 *@s: pointer to the string
 *@b: input
 *@n: input
 * Return: char
 */
char *_memset(char *s, char b, unsigned int n){
	return (0);
}
